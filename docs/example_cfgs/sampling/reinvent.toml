run_type = "sampling"
device = "cpu"  # or "cpu"

[parameters]
model_file = "prior/reinvent.prior"
output_file = "output/sampling_reinvent.csv"
num_smiles = 100
unique_molecules = true
randomize_smiles = true
